// Program demonstrating arithmetic and comparison
void calculate() {
    int a, b;
    a = 5 + 3 * 2; // a = 11
    b = (a - 1) / 2; // b = 5

    if (b <= a) {
        // b is less than or equal to a
    }

    while (b != 0) {
        b = b - 1;
    }
}